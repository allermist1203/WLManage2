
export const MatchType = Object.freeze({
    BO1: 1,
    BO3: 3,
});

export const WinLose = Object.freeze({
    WIN: 1,
    LOSE: 2,
});

export const FirstPlayer = Object.freeze({
    MYSELF: 'myself',
    ENEMY: 'enemy',
});

export const GAMES = Object.freeze({
    bs : 'BattleSprits',
});

export const MATCHINFO_MODE = Object.freeze({
    input : 'mode-input',
});

export const APP_MODE = Object.freeze({
    NEW : 'new',
    LIST: 'list',
    SETTINGS: 'setting',
});